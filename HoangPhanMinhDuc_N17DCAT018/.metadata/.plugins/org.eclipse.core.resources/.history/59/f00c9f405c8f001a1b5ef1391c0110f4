<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib uri="http://java.sun.com/jstl/core_rt" prefix="c" %>
<%@ taglib uri="http://java.sun.com/jstl/fmt_rt" prefix="f" %>
<h2>Đây là file home</h2>

<div class="song-list">
	<ul>
	<c:forEach var="s" items="${songList}">
		<li>
			<div class="song-infor">
				<ul class="main-infor" song-id="${s.getSong().getId()}" role="play" link="${s.getSong().getLink()}">
					<li>
						<div class="song-name">${s.getSong().getSongName()}</div>
						<div class="singer-name">${s.getSong().getSingerName()}</div>
					</li>
					<li>Duration</li>
					<c:if test="${logged }">
						<c:if test="${s.isAdded()}">
							<li role="addPL"><div class="loveBtn">Love</div></li>	
						</c:if>
						<c:if test="!${s.isAdded()}">
							<li role="addPL"><div style="color: black" class="loveBtn">Love</div></li>	
						</c:if>
					</c:if>
				</ul>
			</div>
		</li>
	</c:forEach>
	</ul>
</div>
